This should port the coolprop source to Golang. 
The output should be a PropSI like conversion (and only PropSI). 

The functions below should be pure golang (and no CGO or different).

The Cpp files can be found in the source folder of this project